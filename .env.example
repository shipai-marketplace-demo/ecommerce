# GitHub API Configuration
# Generate a personal access token at: https://github.com/settings/tokens
# Required for fetching pull requests and comments
GITHUB_TOKEN=your-github-token-here

# OpenTelemetry Configuration
# Enable verbose tracing to see additional spans beyond defaults
# Recommended for development and initial debugging
NEXT_OTEL_VERBOSE=1

# Mixedbread Configuration (Optional)
# Get your API key and store ID from https://platform.mixedbread.com
# Or from the Vercel Integration marketplace
# If not configured, the app will only show hardcoded products
MXBAI_API_KEY=your-mixedbread-api-key
MXBAI_STORE_ID=your-mixedbread-store-id
