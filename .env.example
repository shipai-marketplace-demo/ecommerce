# Chatbase Configuration
# Generate a secret key for HMAC-SHA256 hashing
# You can generate a secure secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CHATBASE_SECRET=your-secret-key-here

# GitHub API Configuration
# Generate a personal access token at: https://github.com/settings/tokens
# Required for fetching pull requests and comments
GITHUB_TOKEN=your-github-token-here
